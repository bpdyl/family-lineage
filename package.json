{
  "name": "family-lineage",
  "version": "1.0.0",
  "private": true,
  "description": "Paudyal Family Lineage - Interactive family tree application",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build:frontend": "cd frontend && npm run build",
    "seed": "cd backend && npm run seed",
    "start": "cd backend && npm start",
    "render:build": "npm run install:all && npm run build:frontend && npm run seed"
  }
}
